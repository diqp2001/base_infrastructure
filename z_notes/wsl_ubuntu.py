"""

wsl --version

to see my machines installed :

wsl --list --verbose

to start the installed machine:
wsl -d Ubuntu

exit ubuntu machine :
exit

On Ubuntu machine:

    gh auth login
    gh --version
    before starting claude go to proper repo 
    cd /mnt/c/Users/Philippe/coding/git/base_infrastructure

    Strart claude command :
    claude


"""